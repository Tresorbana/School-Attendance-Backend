# Install this in addition to requirements.txt when using a PostgreSQL DATABASE_URL.
# On Linux (VPS production):
#   pip install -r requirements.txt -r requirements-postgres.txt
-r requirements.txt
psycopg2-binary>=2.9.9
